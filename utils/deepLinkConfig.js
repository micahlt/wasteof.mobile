const config = {
  screens: {
    home: '',
    notifications: 'messages',
    explore: 'explore',
    search: 'search',
    settings: 'settings',
  },
};

const linking = {
  prefixes: ['wasteofmobile://', 'https://wasteof.money'],
  config,
};

export default linking;
